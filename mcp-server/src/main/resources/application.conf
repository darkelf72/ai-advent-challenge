ktor {
    deployment {
        # HTTP port
        port = 8082
        port = ${?PORT}

        # HTTPS/SSL Configuration
        # HTTPS is enabled automatically if keystore.jks exists or SSL_ENABLED=true
        # SSL port: 8443
        #
        # Environment variables for SSL configuration:
        # - SSL_ENABLED: Set to "true" to enable SSL
        # - SSL_KEY_ALIAS: Keystore alias (default: mcpserver)
        # - SSL_KEYSTORE_PASSWORD: Keystore password (default: changeit)
        # - SSL_KEY_PASSWORD: Private key password (default: changeit)
    }
    application {
        modules = [ mcp_server.ApplicationKt.module ]
    }

}
