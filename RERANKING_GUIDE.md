# 🔄 Руководство по реранкингу

## Что реализовано

Добавлен **кросс-энкодер реранкинг** в `VectorSearchService` с возможностью легко включать/выключать через переменные окружения.

### Архитектура

```
Запрос пользователя
    ↓
OllamaRagClient.augmentPromptWithContext()
    ↓
1. Получение embedding для запроса
    ↓
2. VectorSearchService.searchSimilarChunks()
    ├─ Векторный поиск (cosine similarity)
    ├─ Фильтрация по threshold (0.65)
    ├─ Топ-20 кандидатов
    ├─ Кросс-энкодер реранкинг (если USE_RERANKING=true)
    └─ Топ-5 самых релевантных
    ↓
3. Построение контекста
    ↓
4. Формирование RAG промпта
    ↓
Результат
```

---

## ⚡ Быстрый старт

### 1. Включить реранкинг (один шаг!)

В файле `.env` установите:

```bash
USE_RERANKING=true
HUGGINGFACE_API_KEY=hf_your_token_here  # Опционально, но рекомендуется
```

**Готово!** Реранкинг теперь работает автоматически во всех RAG запросах.

### 2. Выключить реранкинг

В файле `.env`:

```bash
USE_RERANKING=false
```

Или просто закомментируйте/удалите эту строку.

---

## 🎯 Использование в коде

Реранкинг **автоматически интегрирован** в `OllamaRagClient`. Вам не нужно менять код!

### Как это работает автоматически:

**1. Dependency Injection настроен автоматически** (см. `AppModule.kt:63-74`):
```kotlin
// RerankerService создается автоматически, если USE_RERANKING=true
single<RerankerService?> {
    val useReranking = System.getenv("USE_RERANKING")?.toBoolean() ?: false
    if (useReranking) {
        RerankerService(
            apiKey = System.getenv("HUGGINGFACE_API_KEY"),
            model = System.getenv("RERANKER_MODEL") ?: "BAAI/bge-reranker-v2-m3"
        )
    } else null
}
```

**2. OllamaRagClient использует реранкинг автоматически** (см. `OllamaRagClient.kt:31-48`):
```kotlin
override suspend fun augmentPromptWithContext(userPrompt: String): String {
    val useReranking = System.getenv("USE_RERANKING")?.toBoolean() ?: false

    val similarChunks = vectorSearchService.searchSimilarChunks(
        queryEmbedding = queryEmbedding,
        queryText = userPrompt,        // Передается автоматически
        useReranking = useReranking    // Контролируется через .env
    )
    // ... дальнейшая обработка
}
```

### Пример использования (код не меняется!)

```kotlin
// Ваш существующий код продолжает работать без изменений:
val ragClient: RagClient = get()  // Koin DI
val augmentedPrompt = ragClient.augmentPromptWithContext("Какая погода в Москве?")

// Реранкинг применяется автоматически, если USE_RERANKING=true
```

### Прямое использование VectorSearchService (если нужно)

Если хотите использовать VectorSearchService напрямую:

```kotlin
val vectorSearchService: VectorSearchService = get()

// С реранкингом
val results = vectorSearchService.searchSimilarChunks(
    queryEmbedding = embedding,
    queryText = "текст запроса",
    useReranking = true
)

// Без реранкинга
val results = vectorSearchService.searchSimilarChunks(
    queryEmbedding = embedding,
    useReranking = false  // или просто не передавать, default = false
)
```

---

## 🔑 Получение API ключа HuggingFace (бесплатно)

1. Зайдите на https://huggingface.co/
2. Создайте бесплатный аккаунт
3. Перейдите в Settings → Access Tokens
4. Создайте новый токен (достаточно read-доступа)
5. Добавьте в переменные окружения:

```bash
export HUGGINGFACE_API_KEY="hf_your_token_here"
```

### Лимиты бесплатного тарифа:
- **Без API ключа**: ~30 запросов/час
- **С бесплатным API ключом**: ~1000 запросов/день

---

## 🤖 Модели для русского языка

### Рекомендуемые модели:

1. **BAAI/bge-reranker-v2-m3** ⭐ (по умолчанию)
   - Multilingual, отлично работает с русским
   - Лучший баланс скорости и точности

2. **BAAI/bge-reranker-large**
   - Выше точность
   - Медленнее работает

3. **BAAI/bge-reranker-base**
   - Быстрее
   - Немного ниже точность

---

## 📊 Когда использовать реранкинг

### ✅ Используйте реранкинг когда:
- Нужна максимальная точность поиска
- Запросы сложные и семантически неоднозначные
- Важно ранжирование результатов
- Есть время на дополнительную обработку (~100-300ms)

### ❌ Не используйте реранкинг когда:
- Нужна максимальная скорость ответа
- Запросы простые
- Бюджет API вызовов ограничен
- Векторный поиск уже дает хорошие результаты

---

## 🔧 Отключение реранкинга

Для отключения реранкинга просто:

### Способ 1: Не передавать RerankerService
```kotlin
val searchService = VectorSearchService(repository)  // Без reranker
```

### Способ 2: Установить useReranking = false
```kotlin
val results = searchService.searchSimilarChunks(
    queryEmbedding = embedding,
    useReranking = false  // ← Отключить
)
```

### Способ 3: Закомментировать создание RerankerService
```kotlin
// val reranker = RerankerService(apiKey)  // ← Закомментировать
val searchService = VectorSearchService(repository, null)
```

---

## 📈 Ожидаемые улучшения

При включении реранкинга ожидается:
- **+10-20% точность** в сложных запросах
- **+50-100ms задержка** на запрос
- Лучшее ранжирование при неоднозначных запросах
- Меньше "шумовых" результатов

---

## 🐛 Troubleshooting

### Ошибка: "Reranker API error"
- Проверьте API ключ HuggingFace
- Убедитесь, что модель доступна
- Проверьте лимиты бесплатного тарифа

### Реранкинг не применяется
- Убедитесь, что передан `queryText`
- Проверьте флаг `useReranking = true`
- Убедитесь, что `rerankerService` не null

### Слишком медленно
- Попробуйте модель `bge-reranker-base` (быстрее)
- Уменьшите `TOP_K_BEFORE_RERANK` (сейчас 20)
- Используйте кэширование результатов

---

## 📝 Примеры изменений в коде

### До (без реранкинга):
```kotlin
val results = vectorSearchService.searchSimilarChunks(queryEmbedding)
```

### После (с реранкингом):
```kotlin
val results = vectorSearchService.searchSimilarChunks(
    queryEmbedding = queryEmbedding,
    queryText = userQuery,
    useReranking = true
)
```

Вот и всё! Реранкинг легко включается и выключается одним флагом.
